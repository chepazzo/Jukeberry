---
# handlers file for jukeberry
- name: restart jukeberry
  become: true
  ansible.builtin.systemd:
    name: jukeberry
    state: restarted

- name: reload udev rules
  become: true
  ansible.builtin.command:
    cmd: udevadm control --reload-rules && udevadm trigger
